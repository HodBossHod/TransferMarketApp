<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mycode.newapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NewApplication">
        <activity android:name=".Login"></activity>
        <activity android:name=".Register" />
        <activity android:name=".HomeStart1" />
        <activity
            android:name=".TabbedPlayerActivity"
            android:label="@string/title_activity_tabbed_player"
            android:theme="@style/Theme.NewApplication.NoActionBar" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".StartMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".MainActivity" />
    </application>

</manifest>